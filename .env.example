# Signs the session cookie. Required in production (Vercel must set this).
# Local/dev may omit it and will use a documented insecure default.
SESSION_SECRET=replace-with-a-long-random-string

# next start / next dev
# PORT=3000
